import 'package:flutter/material.dart';

class Order extends StatelessWidget {
  static const routeName = '/customer/order';

  const Order({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Order");
  }
}